spring:
  application:
    name: backend
  data:
    mongodb:
      uri: mongodb://192.168.1.108:27017/medilabo

server:
  port: 8081

eureka:
  client:
    service-url:
      defaultZone: http://localhost:8010/eureka/
    register-with-eureka: true
    fetch-registry: true
  instance:
    hostname: localhost

logging:
  pattern:
    console: "%d{yyyy-MM-dd HH:mm:ss} [%thread] %-5level %logger{36} - %msg%n"
  level:
    org:
      springframework:
        cloud:
          gateway: DEBUG
          gateway.route: DEBUG